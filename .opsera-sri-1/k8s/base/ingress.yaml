# =============================================================================
# Kubernetes Ingress for sri-1
# Generated by Opsera Code-to-Cloud v0.6
# Domain: agent.opsera.dev
# =============================================================================
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sri-1
  labels:
    app: sri-1
  annotations:
    # For AWS ALB Ingress Controller
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/target-type: ip
    # For NGINX Ingress Controller (if using)
    # kubernetes.io/ingress.class: nginx
    # For ExternalDNS
    external-dns.alpha.kubernetes.io/hostname: agent.opsera.dev
spec:
  rules:
    - host: agent.opsera.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sri-1
                port:
                  number: 80
