# =============================================================================
# Kubernetes Deployment for sri-1
# Generated by Opsera Code-to-Cloud v0.6
# Learning #337: Use startupProbe for apps with slow initialization
# =============================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sri-1
  labels:
    app: sri-1
    app.kubernetes.io/name: sri-1
    app.kubernetes.io/managed-by: kustomize
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sri-1
  template:
    metadata:
      labels:
        app: sri-1
    spec:
      serviceAccountName: sri-1
      containers:
        - name: sri-1
          image: IMAGE_PLACEHOLDER
          ports:
            - containerPort: 8080
              protocol: TCP
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          # Learning #337: startupProbe prevents premature restarts
          startupProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 2
            failureThreshold: 30
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 0
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
            initialDelaySeconds: 0
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            runAsNonRoot: true
            runAsUser: 1001
